# OptiPic — Smart Image Optimizer
# Runtime dependencies (st.App ASGI entrypoint requires Streamlit >= 1.50)
streamlit>=1.50
Pillow>=10.1
numpy>=1.26

# Testing (included because the project ships a test suite)
pytest>=8.0